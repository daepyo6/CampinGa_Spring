<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.campinga.dao.ICampingDao">
	<resultMap  id="campingMap" type="java.util.HashMap"></resultMap>
	<resultMap  id="campingMap2" type="java.util.HashMap"></resultMap>
	
	<select id="getNewRecoCamping"  statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			CALL getNewRecoCamping(
				#{ref_cursor1 ,mode=OUT, jdbcType=CURSOR, javaType=ResultSet, resultMap=campingMap },
				#{ref_cursor2 ,mode=OUT, jdbcType=CURSOR, javaType=ResultSet, resultMap=campingMap2}
			)
		}
	</select>
</mapper>